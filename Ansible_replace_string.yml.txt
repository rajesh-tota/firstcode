---
- name: Change DNS IPs
  hosts: gear
  become: yes
  become_user: root
  tasks:
  - name: Replace multiple lines
    replace:
      path: /etc/resolv.conf
      regexp: '{{item.From}}'
      replace: '{{item.To}}'
    with_items:
      - { From: '10.190.8.3', To: '10.30.0.22'}
      - { From: '10.190.8.2', To: '10.30.0.21'}
